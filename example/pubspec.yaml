name: call_log_example
description: Demonstrates how to use the call_log plugin.
version: 1.0.0+1
publish_to: none

environment:
  sdk: '>=3.7.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.5
  workmanager:
    git:
      url: https://github.com/4Q-s-r-o/flutter_workmanager.git
      path: workmanager
      ref: v0.5.2-f2.29

dev_dependencies:
  flutter_test:
    sdk: flutter

  call_log:
    path: ../

flutter:
  uses-material-design: true